# T00-001 — Repository hiện hữu

- **Thời gian kiểm chứng:** 2026-09-19 (Asia/Saigon)
- **Trạng thái:** complete
- **Phạm vi:** kiểm chứng repository hiện có; không tạo remote mới, đổi tên repository hoặc push.

## Expected

Project là một Git worktree không-bare hợp lệ, có branch/commit nền và identity remote được ghi nhận. Không làm mất hoặc tự động add các thay đổi trong working tree.

## Commands và actual

`git rev-parse --show-toplevel` → `D:/IT-Dev/python-projects/Typhoon_vn`, exit 0.

`git rev-parse --is-inside-work-tree` → `true`, exit 0.

`git rev-parse --is-bare-repository` → `false`, exit 0.

`git branch --show-current` → `main`, exit 0.

`git rev-parse HEAD` → `b5223d4db796008d4dc90e16641cbdf91e95222b`, exit 0.

`git remote get-url origin` và `git remote get-url --push origin` → `https://github.com/VietManh05/typhoon-track-vietnam.git`, exit 0.

## Quyết định scope

Roadmap gọi repository dự kiến là `typhoon-vn-forecast-system`, còn repository thực tế đã có identity `typhoon-track-vietnam`. Mục tiêu chức năng “có repository Git cho dự án” đã đạt. Giữ nguyên repository/remote hiện hữu theo chính sách bảo toàn workspace; việc đổi tên hoặc tạo remote mới cần yêu cầu riêng và không thuộc kiểm chứng cục bộ này.

## Kết luận

PASS. Repository hợp lệ và đã có lịch sử/remote. Không có file sản phẩm hay Git reference nào bị thay đổi trong lúc kiểm chứng.

Task kế tiếp: T00-002 — kiểm chứng/tạo các branch `main`, `develop`.
