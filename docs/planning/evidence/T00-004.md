# T00-004 — .gitignore

- **Thời gian:** 2026-09-19 (Asia/Ho_Chi_Minh)
- **Trạng thái:** complete
- **Input:** tracked `.gitignore`; synthetic path names passed to `git check-ignore` only (no files created).

## Expected and actual

`git check-ignore -v` confirmed ignore rules for Python caches, pytest/coverage, virtual environments, `.env`/`.env.local`, raw/interim/processed/external data, checkpoint `.pt/.ckpt`, MLflow artifacts, DVC cache and logs. Exit was 0 for every ignored fixture.

Negative/allowlist checks confirmed `.env.example`, all data `.gitkeep` files, `checkpoints/.gitkeep`, and source code remain visible (not ignored). The `.gitignore` itself had no diff and required no patch. HEAD/current branch stayed unchanged.

PASS. No fixture, secret, data, checkpoint, add, commit or push was created.

Task kế tiếp: T00-005 — audit README content and executable examples against current implementation.
