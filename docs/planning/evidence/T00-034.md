# T00-034 — Environment

- **Trạng thái:** complete — 2026-09-19.
- `.venv` Python 3.11.9 được dùng; install editable với `api,train,ingestion,dev,experiment` exit 0.
- `python -m pip check` trả `No broken requirements found`.
