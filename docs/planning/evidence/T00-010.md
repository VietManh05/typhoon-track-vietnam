# T00-010 — Chạy lint lần đầu

- **Thời gian:** 2026-09-19 (Asia/Ho_Chi_Minh)
- **Trạng thái:** complete
- **Phạm vi:** `src/`, `tests/`, cấu hình lint hiện có; không stage, commit hoặc push.

Baseline đã ghi nhận `flake8 src tests` thất bại với 33 findings, trong khi `isort --check-only src tests` và `black --check src tests` đều pass. Findings gồm import thừa, dòng E501 và hai F821 trong dataset. Sau các chỉnh sửa có mục tiêu, lần tiếp quản còn 16 F401; đã xóa đúng các import không dùng trong 10 tệp và chạy isort trên 8 tệp cần chuẩn hóa lại nhóm import.

Kết quả lint cuối bằng Python 3.11.9 trong `.venv`:

- `.venv/Scripts/python.exe -m flake8 src tests` — exit 0, không còn finding.
- `.venv/Scripts/python.exe -m isort --check-only src tests` — exit 0.
- `.venv/Scripts/python.exe -m black --check src tests` — exit 0, 74 files unchanged.

Lần full pytest đầu tiên dùng temp mặc định chạy được 62 test rồi báo 13 setup errors vì Windows từ chối truy cập `C:/Users/YOU/AppData/Local/Temp/pytest-of-YOU`; đây là lỗi môi trường của fixture `tmp_path`, không phải test assertion. Chạy lại với basetemp trong workspace:

`.venv/Scripts/python.exe -m pytest --basetemp .pytest-t00-010-evidence --junitxml docs/planning/evidence/T00-010-pytest.xml`

Kết quả: **75 passed, 2 dependency deprecation warnings in 15.33s**, exit 0. JUnit: `docs/planning/evidence/T00-010-pytest.xml`.

Task kế tiếp: T00-011 — commit cấu trúc ban đầu. Task này cần sự cho phép rõ ràng của người dùng trước khi tạo commit.
