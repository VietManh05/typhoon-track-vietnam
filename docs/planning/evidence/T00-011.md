# T00-011 — Commit cấu trúc ban đầu

- **Thời gian:** 2026-09-19 (Asia/Ho_Chi_Minh)
- **Trạng thái:** complete
- **Commit message:** `chore(repo): establish verified project foundation`
- **Target ref:** `codex/g00-foundation`; không push hoặc thay đổi remote.

Trước khi commit, `main`, `develop` và `codex/g00-foundation` cùng trỏ `b5223d4`. Host khóa `.git/HEAD` tại `main`, nên `git switch` và `git symbolic-ref` không thể đổi branch. Để không commit trực tiếp vào `main`, snapshot được tạo từ index đã rà và ref `codex/g00-foundation` được cập nhật bằng commit object mới; local `main` và `origin/main` được giữ nguyên.

Manifest dùng danh sách đường dẫn tường minh. Snapshot gồm source, tests, planning/evidence và cấu hình đã kiểm chứng từ lát cắt R10–R14 cùng T00-001–T00-010. Các artefact ngoài phạm vi không được commit: `.grapuco/`, các báo cáo `.txt`, file `.docx` đang bị xóa, temp index/pytest và frontend xuất hiện đồng thời.

Kiểm chứng trước commit:

- `python -m pre_commit run` trên staged snapshot: tất cả hooks pass.
- `python -m pytest --basetemp .pytest-t00-011-final`: **75 passed**, 2 dependency deprecation warnings, exit 0.
- `python docs/planning/tools/validate_plan.py`: pass; 818/818 source checkboxes mapped, source hash unchanged, không có error.
- Conventional Commit validator với message trên: pass.
- `git diff --cached --check`: pass.

Task kế tiếp: T00-012 — kiểm chứng/tạo `data/raw`. Không push commit nếu chưa có yêu cầu riêng.
