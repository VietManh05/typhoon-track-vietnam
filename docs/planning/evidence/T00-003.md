# T00-003 — Feature branch

- **Thời gian kiểm chứng:** 2026-09-19 (Asia/Saigon)
- **Trạng thái:** complete
- **Base:** `develop` tại `b5223d4db796008d4dc90e16641cbdf91e95222b`

## Expected

Có một local branch theo feature với prefix của workspace; không checkout, không push và không làm thay đổi working tree.

## Thao tác

`git branch codex/g00-foundation develop` → exit 0.

## Actual

`git branch --list --verbose --no-abbrev` xác nhận `codex/g00-foundation`, `develop` và current branch `main` cùng trỏ `b5223d4db796008d4dc90e16641cbdf91e95222b`. Không cấu hình upstream và không push remote.

## Kết luận

PASS. Feature ref được tạo cục bộ theo convention `codex/<feature>` mà không tác động checkout hiện tại.

Task kế tiếp: T00-004 — kiểm chứng và hoàn thiện `.gitignore`.
