# S01 — Version hóa dữ liệu bằng DVC

- **Trạng thái:** complete.
- **Ngày:** 2026-09-19.
- **Implementation:** `.dvc/`, `dvc.yaml`, `dvc.lock`, synthetic input `tests/fixtures/phase2_track.csv`, deterministic builder và ba output ignored dưới `data/processed/`; remote chỉ cấu hình local qua `DVC_REMOTE_URL`.
- **Lệnh:** `.venv\Scripts\dvc.exe repro phase2-fixture`; `.venv\Scripts\dvc.exe status`.
- **Actual:** lần đầu sinh lock/output; lần hai `Stage 'phase2-fixture' didn't change, skipping`; status `Data and pipelines are up to date`.
- **An toàn:** không cấu hình remote, không `dvc push`, không track dữ liệu third-party thật.
