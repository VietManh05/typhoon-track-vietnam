# T00-009 — pre-commit

- **Thời gian:** 2026-09-19 (Asia/Ho_Chi_Minh)
- **Trạng thái:** complete
- **Files:** `.pre-commit-config.yaml`, `Makefile`, `CONTRIBUTING.md`, `tests/test_precommit_config.py`.

Configured file-stage hooks (large files, conflicts, TOML/YAML, EOF, whitespace, Black, isort, Flake8) and the local Conventional Commit `commit-msg` hook. Setup now installs both `pre-commit` and `commit-msg` stages. `pre-commit 4.6.2` validated the configuration and installed `.git/hooks/pre-commit` plus `.git/hooks/commit-msg`.

The first behavior test correctly failed because pre-commit refuses an unstaged configuration in the intentionally dirty real worktree. The test was corrected—not bypassed—to copy config/validator into an isolated temporary Git repository, stage only those fixtures, and verify valid commit exit 0 plus invalid commit exit 1.

Focused result: **11 passed in 86.53s**, hooks present. Full regression: **75 passed, 2 dependency deprecation warnings in 16.76s**, exit 0. JUnit: `docs/planning/evidence/T00-009-pytest.xml`. No project files were staged, committed, reset, or pushed.

Task kế tiếp: T00-010 — run the initial lint gate and classify/fix repository-owned violations.
