# T00-012 — Tạo `data/raw`

- **Thời gian:** 2026-09-19 (Asia/Ho_Chi_Minh)
- **Trạng thái:** complete
- **Phạm vi:** kiểm chứng cấu trúc và ignore contract; không tạo, đọc sâu, xóa hoặc ghi đè dữ liệu raw hiện có.

Kết quả kiểm chứng:

- `Test-Path data/raw -PathType Container` trả `True`.
- `git ls-files --error-unmatch -- data/raw/.gitkeep` trả `data/raw/.gitkeep`, exit 0.
- `git check-ignore --no-index -q data/raw/t00-012-fixture.nc` exit 0: dữ liệu raw mới bị ignore bởi `data/raw/*`.
- `git check-ignore --no-index -q data/raw/.gitkeep` exit 1: placeholder không bị ignore nhờ `!data/raw/.gitkeep`.
- `git status --short --untracked-files=all -- data/raw` không có output: không phát sinh thay đổi trong thư mục.

Thư mục đã tồn tại cùng các namespace con `downloads`, `environment`, `geospatial`, `lineage`, `observations`; đây có thể là dữ liệu/workspace cục bộ nên được giữ nguyên và không đưa vào Git. Không cần sửa `.gitignore` hoặc tạo implementation trùng lặp.

Task kế tiếp: T00-013 — kiểm chứng/tạo `data/interim`.
