# T00-032 — Khóa Python version

- **Trạng thái:** complete — 2026-09-19.
- `.python-version` là `3.11.9`; runtime `.venv` báo Python 3.11.9.
- `pyproject.toml` khóa `requires-python = ">=3.11,<3.12"`; contract test pass.
