# T00-008 — Conventional Commits

- **Thời gian:** 2026-09-19 (Asia/Ho_Chi_Minh)
- **Trạng thái:** complete
- **Files:** `scripts/validate_commit_msg.py`, `tests/test_commit_message.py`, `.pre-commit-config.yaml`.

Implemented a dependency-free commit-message validator and registered it as a local `commit-msg` pre-commit stage. Allowed types match CONTRIBUTING; optional lowercase scope, `!`, 72-character header, blank body separator, and `BREAKING CHANGE:` footer are validated. A `!` header requires the breaking footer.

Verification covered three valid messages (including breaking change), seven invalid boundaries, and CLI exit behavior. Actual: `10 passed in 0.08s; CLI valid message exit 0, invalid message exit 1`; PASS, exit 0 for the combined verification. No commit, hook installation, checkout, or push was performed.

Task kế tiếp: T00-009 — verify/install pre-commit configuration without modifying user Git hooks unless explicitly safe.
