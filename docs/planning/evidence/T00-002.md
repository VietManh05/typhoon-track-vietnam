# T00-002 — Branch `main` và `develop`

- **Thời gian kiểm chứng:** 2026-09-19 (Asia/Saigon)
- **Trạng thái:** complete
- **Base commit:** `b5223d4db796008d4dc90e16641cbdf91e95222b`

## Expected

Local repository có refs `main` và `develop`. Việc tạo `develop` không checkout, không đổi working tree và không push remote.

## Trước thay đổi

`git for-each-ref --format='%(refname:short)|%(objectname)|%(upstream:short)' refs/heads` chỉ trả `main|b5223d...|origin/main`. Remote refs chỉ có `origin/main`.

## Thao tác

`git branch develop HEAD` → exit 0.

## Actual

`git branch --show-current` vẫn trả `main`.

Local refs sau thay đổi:

- `main|b5223d4db796008d4dc90e16641cbdf91e95222b|origin/main`
- `develop|b5223d4db796008d4dc90e16641cbdf91e95222b|`

`develop` chỉ là local branch, chưa có upstream và không được push. Không checkout nên các thay đổi đang có trong working tree không bị di chuyển hoặc ghi đè.

## Kết luận

PASS. Hai branch nền tồn tại cục bộ; `main` giữ tracking hiện tại, `develop` được tạo an toàn từ HEAD.

Task kế tiếp: T00-003 — chốt/tạo branch theo feature mà không can thiệp working tree đang có thay đổi đồng thời.
